# Boreal Adapted Video Series

This file translates the three-video brief into Boreal-accurate cuts grounded in the shipped `next-app` surface.

## Shared Design Direction

- Headline font: `Syne`
- Body font: `Manrope`
- Technical labels: `Geist Mono` with `IBM Plex Mono` fallback
- Product surfaces: near-white, charcoal, cyan-teal, pale aqua
- Wrapper scenes: deep graphite with restrained cyan glow

## Video 1 — Demo 90

Goal: show one request moving from ask to route to delivered evidence.

Key adaptations:

- keep the request prompt and matched supply structure
- remove `escrow locked` and `released` claims
- show `provider-backed`, `payment-aware`, `evidence attached`, and `delivered`
- keep match scoring because the app already exposes it

## Video 2 — Update 120

Goal: founder-style builder update for the alpha.

Key adaptations:

- keep the split between `what is live`, `what it's for`, and `what's next`
- replace `BM25 + vector + LLM rerank` with a more honest matching-layer description
- replace quarter-by-quarter certainty with roadmap-now language
- keep `Privy-backed x402` because the roadmap and app docs support it

## Video 3 — Launch 60

Goal: public-facing stop-scroll cut.

Key adaptations:

- keep the hook and the three-step request -> supply -> outcome rhythm
- use `public alpha` and `live today`
- remove `escrow released`
- keep Boreal's category line and final brand close
