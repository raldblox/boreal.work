export const SPECIALIST_FUNDED_START_USDC_AMOUNT = 0.01;
export const SPECIALIST_FRONTEND_WALLET_SOL_AMOUNT = 0.0001;

export function formatUsdcPriceLabel(amount: number) {
  return `${amount} USDC`;
}

export function formatSolPriceLabel(amount: number) {
  return `${amount} SOL`;
}
