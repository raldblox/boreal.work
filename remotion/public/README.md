# Public Assets

Store Remotion runtime assets here when they need to be loaded with `staticFile()`.

Use this folder for:

- music and voiceover files
- logos rendered in the video
- exported screenshots or short clip assets used directly by the compositions

Keep `assets/` for raw source material and `public/` for assets that the Remotion runtime should serve directly.
